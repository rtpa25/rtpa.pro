import { Element } from 'react-scroll';
import Work from '../components/work';

const WorkSection = () => {
    return (
        <Element name='work'>
            <Work />
        </Element>
    );
};

export default WorkSection;
